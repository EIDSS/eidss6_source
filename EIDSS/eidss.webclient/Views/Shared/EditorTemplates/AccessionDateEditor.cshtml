@using Kendo.Mvc.UI
@model DateTime?

@*TODO: Chrome date problem fix - start*@
@*old version@(Html.Kendo().DatePicker()
    .Name("datAccession")
    .Value(Model.HasValue && Model.Value > DateTime.MinValue ? Model : DateTime.Today)
    .HtmlAttributes(new { @class="smallGridEditor" })
    )*@
@(Html.Kendo().DatePicker()
    .Name("datAccession")
    .Value(Model.HasValue && Model.Value > DateTime.MinValue ? Model : DateTime.Today)
    .HtmlAttributes(new { @class = "smallGridEditor", type = "text" })
)
@*TODO: Chrome date problem fix - end*@
